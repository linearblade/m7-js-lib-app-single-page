# handle_later

[README](../README.md) -> [handle_later](./README.md)

Quick notes for deferred cutover items:

- `atLib.js` is the primary source snapshot used for extraction.
- `at_v098.js` is not addressed yet.
- `localStorage.js` is not addressed yet.
- `delagator.js` is obfuscated and not part of current 1.0 wiring.
